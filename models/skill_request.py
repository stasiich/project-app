class SkillRequest:
    def __init__(self, from_email, to_email, course_title, message):
        self.from_email = from_email
        self.to_email = to_email
        self.course_title = course_title
        self.message = message
